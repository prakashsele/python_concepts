# List of python modules based on the python version
# docs.python.org

# two types of modules
# Buildin modules
# external modules(in lib folder in external) = stored in same folder we installed in python folder


# pip --version
# pip install module name (install an private module)
# it will be under site packages in lib

# if you want to uninstall
# pip -uninstall module name


import copy

print(copy.error)